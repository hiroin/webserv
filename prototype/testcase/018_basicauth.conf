server {
  listen *:8080;
  root /tmp/webserv/base;
	index index.html;
}

server {
  listen *:8081;
  root /tmp/webserv/base;
	auth_basic "Restricted area";
	auth_basic_info user:password;
	index index.html;
}

server {
  listen *:8082;
  root /tmp/webserv/base;
	index index.html;
	auth_basic "Restricted area";
	auth_basic_info user:あ;
}